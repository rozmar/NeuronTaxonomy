% This function creates a constant "every element in the same group"
% classification.
function categoryVector = singleCategory(~, triggerVector, ~)

  % Constant category vector
  categoryVector = ones(size(triggerVector));
  
end

