function [cfm] = runTrainingProcess(inputDirs, classLabels, tenfoldSize)
  
  [templates, classes] = loadTemplates(inputDirs, classLabels);
  
  %cutOffTestSet - put into test set the other class and the same size from the positive class
  
  %crossValidation within for loop
    %train on 
  
  
  
end