% isSave Decides if we need to save the result structures and the pictures.
% It will be set in the parameter checking phase of analysis.
function answer = isSave(parameters)
  answer = parameters.isSave;
end